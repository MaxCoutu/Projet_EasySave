<UserControl x:Class="Projet.Wpf.View.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:Projet.Wpf.View"
      xmlns:converters="clr-namespace:Projet.Wpf.View.Converters"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="1000">
    <UserControl.Resources>
        <!-- Converters -->
        <converters:BlockedPackageConverter x:Key="BlockedPackageConverter"/>
        <converters:CryptoExtensionConverter x:Key="CryptoExtensionConverter"/>
        <converters:PriorityExtensionConverter x:Key="PriorityExtensionConverter"/>
        
        <!-- ScrollBar Style -->
        <Style x:Key="CustomScrollBarStyle" TargetType="{x:Type ScrollBar}">
            <Setter Property="Background" Value="#33FFFFFF"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="GridRoot" Width="12" Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Opacity="0" Focusable="False"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Opacity="0" Focusable="False"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{DynamicResource CustomThumbStyle}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Horizontal ScrollBar Style -->
        <Style x:Key="CustomHorizontalScrollBarStyle" TargetType="{x:Type ScrollBar}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="GridRoot" Height="16" Background="Transparent" SnapsToDevicePixels="True">
                            <Border CornerRadius="8" Background="#33FFFFFF" Margin="15,0,15,0"/>
                            <Track x:Name="PART_Track" Orientation="Horizontal" Margin="15,0,15,0">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.LineLeftCommand}" Opacity="0" Focusable="False"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.LineRightCommand}" Opacity="0" Focusable="False"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{DynamicResource CustomHorizontalThumbStyle}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="CustomThumbStyle" TargetType="{x:Type Thumb}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Rectangle Fill="#7428D7" RadiusX="6" RadiusY="6" Margin="2,0,2,0" MinHeight="30"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="CustomHorizontalThumbStyle" TargetType="{x:Type Thumb}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Rectangle Fill="#7428D7" RadiusX="8" RadiusY="8" Margin="0,1,0,1" MinWidth="100"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="CustomScrollViewerStyle" TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <ScrollContentPresenter Grid.Column="0" Grid.Row="0" Margin="0,0,0,0"/>
                            
                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       Grid.Column="1" Grid.Row="0"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                       Style="{DynamicResource CustomScrollBarStyle}"/>
                            
                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                                       Orientation="Horizontal"
                                       Grid.Column="0" Grid.Row="1"
                                       Value="{TemplateBinding HorizontalOffset}"
                                       Maximum="{TemplateBinding ScrollableWidth}"
                                       ViewportSize="{TemplateBinding ViewportWidth}"
                                       Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                                       Style="{DynamicResource CustomHorizontalScrollBarStyle}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Button Styles -->
        <Style x:Key="CircularButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse Fill="{TemplateBinding Background}"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="FontSize" Value="20"/>
        </Style>
        
        <Style x:Key="DeleteButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="Transparent">
                            <Viewbox Width="20" Height="20">
                                <Canvas Width="24" Height="24">
                                    <Path Fill="#FFCC4444" Data="M6,19c0,1.1,0.9,2,2,2h8c1.1,0,2-0.9,2-2V7H6V19z M19,4h-3.5l-1-1h-5l-1,1H5v2h14V4z"/>
                                </Canvas>
                            </Viewbox>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- TextBox Style -->
        <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Padding="{TemplateBinding Padding}"
                                          Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Category Title Style -->
        <Style x:Key="CategoryTitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
        
        <!-- List Item Style -->
        <Style x:Key="ListItemStyle" TargetType="Border">
            <Setter Property="Background" Value="#33FFFFFF"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="Padding" Value="12,8"/>
        </Style>
        
        <!-- Item Text Style -->
        <Style x:Key="ItemTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
        
        <!-- Slider Style -->
        <Style x:Key="CustomSliderStyle" TargetType="Slider">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Track Background -->
                            <Border x:Name="PART_TrackBackground" 
                                    Grid.Row="1" 
                                    Height="4" 
                                    CornerRadius="2" 
                                    Background="#33FFFFFF"
                                    VerticalAlignment="Center"/>
                            
                            <!-- Selection Range -->
                            <Border x:Name="PART_SelectionRange" 
                                    Grid.Row="1" 
                                    Height="4" 
                                    CornerRadius="2" 
                                    Background="#7428D7"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"/>
                            
                            <!-- Thumb -->
                            <Thumb x:Name="Thumb" 
                                   Grid.Row="1" 
                                   Width="16" 
                                   Height="16">
                                <Thumb.Template>
                                    <ControlTemplate TargetType="Thumb">
                                        <Ellipse Fill="#7428D7" Stroke="#FFFFFF" StrokeThickness="1"/>
                                    </ControlTemplate>
                                </Thumb.Template>
                            </Thumb>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Thumb" Property="Width" Value="18"/>
                                <Setter TargetName="Thumb" Property="Height" Value="18"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- ComboBox Style -->
        <Style x:Key="CustomComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#CC19173D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#7F19173D"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          Padding="{TemplateBinding Padding}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" 
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="5">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0" 
                                                                  Content="{TemplateBinding Content}"
                                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                  HorizontalAlignment="Left"
                                                                  VerticalAlignment="Center"
                                                                  Margin="5,0,0,0"/>
                                                <Path Grid.Column="1" 
                                                      x:Name="Arrow"
                                                      Fill="White"
                                                      Data="M0,0 L4,4 L8,0 Z"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                                <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"
                                                  Margin="5,0,0,0"/>
                            </ToggleButton>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder"
                                        Background="#FF302E66"
                                        BorderBrush="#7F19173D"
                                        BorderThickness="1"
                                        CornerRadius="5"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer Margin="0,2,0,2" SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid Background="#FF25234E">
        <!-- Main Layout Grid -->
        <Grid Width="660" HorizontalAlignment="Center">
            <!-- Title -->
            <TextBlock Text="Settings" 
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="#CCFFFFFF"
                       Opacity="0.8"
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Top"
                       Margin="0,10,0,0"/>
            
            <!-- Scrollable Container -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" 
                          HorizontalScrollBarVisibility="Auto"
                          Margin="0,50,0,0"
                          Style="{StaticResource CustomScrollViewerStyle}">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <!-- Blocking Programs Section -->
                    <Border Background="#FF302E66" 
                            CornerRadius="15" 
                            Padding="20"
                            Margin="0,0,15,0"
                            Width="280"
                            Height="360"
                            VerticalAlignment="Top">
                        <DockPanel>
                            <!-- Section Title -->
                            <TextBlock Text="Blocking Programs" 
                                       Style="{StaticResource CategoryTitleStyle}"
                                       DockPanel.Dock="Top"/>
                            
                            <!-- Input Area -->
                            <Grid DockPanel.Dock="Top" Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBox x:Name="BlockingProgramInput"
                                         Grid.Column="0"
                                         Background="#CC19173D"
                                         BorderBrush="#7F19173D"
                                         Foreground="#7B78AA"
                                         BorderThickness="0"
                                         Padding="10"
                                         Text="Process Name" 
                                         Tag="Process Name" 
                                         GotFocus="TextBox_GotFocus"
                                         LostFocus="TextBox_LostFocus"
                                         Height="40" 
                                         FontSize="14"
                                         Style="{StaticResource RoundedTextBoxStyle}"/>
                                         
                                <Button Grid.Column="1" 
                                        Content="+" 
                                        Width="40" 
                                        Height="40"
                                        Background="#7428D7"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Style="{StaticResource CircularButtonStyle}"
                                        Command="{Binding AddBlockingProgramCommand}"
                                        CommandParameter="{Binding ElementName=BlockingProgramInput, Path=Text}"
                                        Click="AddBlockingProgram_Click"
                                        Margin="10,0,0,0"/>
                            </Grid>
                            
                            <!-- List of Items -->
                            <ScrollViewer DockPanel.Dock="Top" 
                                          VerticalScrollBarVisibility="Auto" 
                                          Height="260"
                                          Style="{StaticResource CustomScrollViewerStyle}">
                                <ItemsControl ItemsSource="{Binding BlockedPackages}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource ListItemStyle}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    
                                                    <TextBlock Grid.Column="0" 
                                                               Text="{Binding}" 
                                                               Style="{StaticResource ItemTextStyle}"/>
                                                               
                                                    <Button Grid.Column="1" 
                                                            Style="{StaticResource DeleteButtonStyle}"
                                                            Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ItemsControl}}}"
                                                            CommandParameter="{Binding Converter={StaticResource BlockedPackageConverter}}"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </DockPanel>
                    </Border>
                    
                    <!-- Encrypt Extensions Section -->
                    <Border Background="#FF302E66" 
                            CornerRadius="15" 
                            Padding="20"
                            Margin="0,0,15,0"
                            Width="280"
                            Height="360"
                            VerticalAlignment="Top">
                        <DockPanel>
                            <!-- Section Title -->
                            <TextBlock Text="Extensions to Encrypt" 
                                       Style="{StaticResource CategoryTitleStyle}"
                                       DockPanel.Dock="Top"/>
                            
                            <!-- Input Area -->
                            <Grid DockPanel.Dock="Top" Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBox x:Name="ExtensionInput"
                                         Grid.Column="0"
                                         Background="#CC19173D"
                                         BorderBrush="#7F19173D"
                                         Foreground="#7B78AA"
                                         BorderThickness="0"
                                         Padding="10"
                                         Text="File extension name (.txt)" 
                                         Tag="File extension name (.txt)" 
                                         GotFocus="TextBox_GotFocus"
                                         LostFocus="TextBox_LostFocus"
                                         Height="40" 
                                         FontSize="14"
                                         Style="{StaticResource RoundedTextBoxStyle}"/>
                                         
                                <Button Grid.Column="1" 
                                        Content="+" 
                                        Width="40" 
                                        Height="40"
                                        Background="#7428D7"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Style="{StaticResource CircularButtonStyle}"
                                        Command="{Binding AddExtensionCommand}"
                                        CommandParameter="{Binding ElementName=ExtensionInput, Path=Text}"
                                        Click="AddExtension_Click"
                                        Margin="10,0,0,0"/>
                            </Grid>
                            
                            <!-- List of Items -->
                            <ScrollViewer DockPanel.Dock="Top" 
                                          VerticalScrollBarVisibility="Auto" 
                                          Height="260"
                                          Style="{StaticResource CustomScrollViewerStyle}">
                                <ItemsControl ItemsSource="{Binding CryptoExtensions}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource ListItemStyle}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    
                                                    <TextBlock Grid.Column="0" 
                                                               Text="{Binding}" 
                                                               Style="{StaticResource ItemTextStyle}"/>
                                                               
                                                    <Button Grid.Column="1" 
                                                            Style="{StaticResource DeleteButtonStyle}"
                                                            Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ItemsControl}}}"
                                                            CommandParameter="{Binding Converter={StaticResource CryptoExtensionConverter}}"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </DockPanel>
                    </Border>
                    
                    <!-- Priority Extensions Section -->
                    <Border Background="#FF302E66" 
                            CornerRadius="15" 
                            Padding="20"
                            Margin="0,0,15,0"
                            Width="280"
                            Height="360"
                            VerticalAlignment="Top">
                        <DockPanel>
                            <!-- Section Title -->
                            <TextBlock Text="Priority Extensions" 
                                       Style="{StaticResource CategoryTitleStyle}"
                                       DockPanel.Dock="Top"/>
                            
                            <!-- Input Area -->
                            <Grid DockPanel.Dock="Top" Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBox x:Name="PriorityExtensionInput"
                                         Grid.Column="0"
                                         Background="#CC19173D"
                                         BorderBrush="#7F19173D"
                                         Foreground="#7B78AA"
                                         BorderThickness="0"
                                         Padding="10"
                                         Text="File extension name (.txt)" 
                                         Tag="File extension name (.txt)" 
                                         GotFocus="TextBox_GotFocus"
                                         LostFocus="TextBox_LostFocus"
                                         Height="40" 
                                         FontSize="14"
                                         Style="{StaticResource RoundedTextBoxStyle}"/>
                                         
                                <Button Grid.Column="1" 
                                        Content="+" 
                                        Width="40" 
                                        Height="40"
                                        Background="#7428D7"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Style="{StaticResource CircularButtonStyle}"
                                        Command="{Binding AddPriorityExtensionCommand}"
                                        CommandParameter="{Binding ElementName=PriorityExtensionInput, Path=Text}"
                                        Click="AddPriorityExtension_Click"
                                        Margin="10,0,0,0"/>
                            </Grid>
                            
                            <!-- List of Items -->
                            <ScrollViewer DockPanel.Dock="Top" 
                                          VerticalScrollBarVisibility="Auto" 
                                          Height="260"
                                          Style="{StaticResource CustomScrollViewerStyle}">
                                <ItemsControl ItemsSource="{Binding PriorityExtensions}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource ListItemStyle}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    
                                                    <TextBlock Grid.Column="0" 
                                                               Text="{Binding}" 
                                                               Style="{StaticResource ItemTextStyle}"/>
                                                               
                                                    <Button Grid.Column="1" 
                                                            Style="{StaticResource DeleteButtonStyle}"
                                                            Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ItemsControl}}}"
                                                            CommandParameter="{Binding Converter={StaticResource PriorityExtensionConverter}}"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </DockPanel>
                    </Border>
                    
                    <!-- Max File Size Section -->
                    <Border Background="#FF302E66" 
                            CornerRadius="15" 
                            Padding="20"
                            Margin="0,0,0,0"
                            Width="280"
                            Height="360"
                            VerticalAlignment="Top">
                        <DockPanel>
                            <!-- Section Title -->
                            <TextBlock Text="Maximum File Size" 
                                       Style="{StaticResource CategoryTitleStyle}"
                                       DockPanel.Dock="Top"/>
                            
                            <!-- Input Area -->
                            <Grid DockPanel.Dock="Top" Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBox x:Name="MaxFileSizeInput"
                                         Grid.Column="0"
                                         Background="#CC19173D"
                                         BorderBrush="#7F19173D"
                                         Foreground="#7B78AA"
                                         BorderThickness="0"
                                         Padding="10"
                                         Text="{Binding FileSizeValue, UpdateSourceTrigger=PropertyChanged}" 
                                         Height="40" 
                                         FontSize="14"
                                         Style="{StaticResource RoundedTextBoxStyle}"/>
                                         
                                <ComboBox Grid.Column="1" 
                                          Width="70" 
                                          Height="40"
                                          Background="#CC19173D"
                                          Foreground="#7B78AA"
                                          BorderBrush="#7F19173D"
                                          BorderThickness="0"
                                          ItemsSource="{Binding FileSizeUnits}"
                                          SelectedIndex="{Binding SelectedUnitIndex}"
                                          Margin="10,0,0,0">
                                    <ComboBox.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="10"/>
                                        </Style>
                                    </ComboBox.Resources>
                                    <ComboBox.ItemContainerStyle>
                                        <Style TargetType="ComboBoxItem">
                                            <Setter Property="Padding" Value="8,4"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                        </Style>
                                    </ComboBox.ItemContainerStyle>
                                </ComboBox>
                            </Grid>
                            
                            <!-- Apply Button -->
                            <Button Content="Apply" 
                                    Height="40"
                                    Background="#7428D7"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Command="{Binding ApplyFileSizeCommand}"
                                    DockPanel.Dock="Top"
                                    Margin="0,0,0,15">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="10"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                            
                            <!-- Current Value Display -->
                            <Border Background="#33FFFFFF" 
                                    CornerRadius="5" 
                                    Padding="12,8" 
                                    Margin="0,0,0,10" 
                                    DockPanel.Dock="Top">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Grid.Column="0"
                                               Text="Current value: "
                                               Foreground="#7B78AA"
                                               FontSize="14"/>
                                               
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding MaxFileSizeDisplay}"
                                               Foreground="White"
                                               FontSize="14"
                                               FontWeight="SemiBold"/>
                                </Grid>
                            </Border>
                            
                            <!-- Helper Text -->
                            <TextBlock Text="Maximum size of files that can be transferred simultaneously" 
                                       Foreground="#7B78AA"
                                       TextWrapping="Wrap"
                                       Margin="0,5,0,0"
                                       FontSize="12"
                                       DockPanel.Dock="Top"/>
                        </DockPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>