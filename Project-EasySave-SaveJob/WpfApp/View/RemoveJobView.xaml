<Window x:Class="Projet.Wpf.View.RemoveJobView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Supprimer une tâche" Height="300" Width="400">
    <Grid Margin="10">
        <ListBox ItemsSource="{Binding Jobs}" SelectedItem="{Binding SelectedJob}" DisplayMemberPath="Name" />
        <Button Content="Supprimer" VerticalAlignment="Bottom" Height="30" Margin="0,10,0,0" Command="{Binding RemoveCmd}" />
    </Grid>
</Window>
